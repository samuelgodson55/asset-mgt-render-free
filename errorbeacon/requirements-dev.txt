# Test-only dependencies. Not installed in the production image (see
# Dockerfile / requirements.txt) -- only needed to run tests/test_core.py.
-r requirements.txt
pytest
httpx  # required by fastapi.testclient.TestClient (starlette.testclient)
